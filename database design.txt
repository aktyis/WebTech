**the primary keys are _(dash)ed in the front like _id. But user_name is not a primary key


users(user_id,password,user_status,image)

students(user_id,student_id(unique),first_name,last_name,contact_number,email,blood_group,current_address,gender,permanent_address,guardians_name)

courses(_course_id,discipline_id,name,is_open,total_capacity,filled_number,duration,start_time,end_time,day,room,sec,faculty_name)

taken_courses(_student_id,_course_id,completion_status,availablity_status,semester,date,cpga)

faculties(user_id,faculty_id(unique),name,qulification,contact_no,email,specility)
